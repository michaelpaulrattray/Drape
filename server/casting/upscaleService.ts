/**
 * Paid export upscaling was removed by D-74. Keep this tombstone temporarily
 * so mixed-version deployments cannot accidentally resolve an old import to a
 * callable credit-spending service. There is deliberately no exported API.
 */
