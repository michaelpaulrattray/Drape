/**
 * Drape™ Design System Components
 *
 * What is left of the marketing-page kit: the conveyor `Button`, which
 * `Login.tsx` uses. `Section`, `Card`, `Grid` and the typography set went with
 * the hero-only homepage (b8e8a2d9, 2026-04-03) and were removed once nothing
 * imported them (#108 slice 4). Import from this file for convenience.
 *
 * @example
 * import { Button } from "@/components/design-system";
 */

export { Button } from "./Button";
