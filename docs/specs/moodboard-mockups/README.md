# Mood board mockups (card #1210 — DEFERRED 2026-09-25)

The five boards settled with the founder on 2026-09-25, kept here so the design does
not depend on a link. Published copy: https://claude.ai/artifact/6ptnqDJDB7kJw2BZNf6owT

- `Main.dc.html` — the hero with the concept and mood board entrances
- `Concept.dc.html` — Upload a concept: editable brief, the refresh icon, the `@` row
- `Moodboard.dc.html` — the library (Pinterest-style, Create card)
- `Board.dc.html` — one board open, same frame as the library
- `Mention.dc.html` — the `@` picker on a brief
- `canvas.json` — the canvas layout

Every string on them is quotation, not requirement (UI completion contract): re-derive
against capability truth before shipping. The compose rules, the 20-picture cap, the
collage attachment and the courts are on #1210, which holds the spec. His word:
*"lets defer this for now we can build it later"* — nothing is built until he brings it back.
