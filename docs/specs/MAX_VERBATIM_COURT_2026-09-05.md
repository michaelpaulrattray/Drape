# THE MAX VERBATIM COURT — #477 (+ #252's instruction half), run 2026-09-05

**His ruling is the spec (Crew reply #114, 2026-09-03, verbatim and entire):**
*"A — keep Sonnet, and read Grok's verbatim discipline as evidence for
tightening MAX's instruction rather than swapping the engine"*

The engine did not move (`AUTHOR_MODEL` unchanged). The instruction did, in
four places, and this is the before/after #327's bar requires of any MAX
instruction change — *context is not additive* is his own measured law, and a
tightening can produce a THINNER sheet as easily as a truer one. **His eye on
the strip is the gate; every number here is a pointer.**

## What moved in the instruction (guards untouched — his (b) bar reserved)

1. **FACTS STAY opens "IN THE REQUEST'S OWN WORDS"** and the old *"in your
   own sentence"* — an instruction to re-describe — is gone. Evidence: the
   #466 bench measured the shipped author re-describing the same facts in
   +124..+222 words of its own prose, with every #327-class miss (skin-stack,
   block brushes, jewellery listing) riding inside that prose, while the
   verbatim arm held +14..+44 and hit none of them.
2. **TASTE GOES UP, IN TIGHT CLAUSES** — heat sits beside the request's words,
   never as a second description.
3. **The golden heat example lost one word.** It taught *"No soft youthful
   rounding"* and `ageContradictionIn` refuses "youthful" whole-word on any
   30s+ seed, denial or not — driven before the edit: `draftRefusal` on the
   instruction's own taught sentence at a mid-40s stated age returns the age
   refusal. #466 watched both arms borrow the phrase nearly verbatim; #252's
   floor read 5/5 stated-age refusal events as an added "youthful". His
   original sentence stays verbatim in the source comment as origin.
4. **The denial clauses.** `skinContradictionIn` has no denial-awareness
   (driven: *"Real textured skin, not waxy or airbrushed."* is refused), and
   7/16 of #252's refusal events were words SKIN_RULE itself enumerates — the
   echo's natural shape is a denial. SKIN_RULE and FACTS STAY now say so:
   never a refused word, even to deny it; say what the thing IS.

A new suite arm (`creativeRegisterScope.test.ts`, "#477") pins every
taught-good example string to the source with `toContain` and drives it
through the full real `draftRefusal` chain at the least favourable stated
facts — the class is closed mechanically, whatever the phrase and whichever
guard. Its positive control is the removed sentence itself.

## The arms

Same sitting, real `authorPrompt` at MAX on the author's own engine (Sonnet),
BEFORE from main's tree, AFTER from the branch. Fixtures: **his 553-char
cyborg brief** (n=8/arm — #327's fixture, the frames bar) and **MAN 2** (the
shipped reader's own description, byte-for-byte from the #231 record,
n=6/arm) — the fixture where the #466 bench watched the shipped author lose a
customer's MAX to age words; a cyborg-only null on the refusal half would be
a null without a fixture that could produce it. Refusal sentences recorded
VERBATIM (#529's `refusals[]` — the 300-char-prefix blindness #252 named is
gone). Driver `scripts/_477-court-disposable.mts`, reader
`scripts/_477-court-read-disposable.mts`, record `output/477-max/`.

## The refusal half — #252(c), "fewer drafts die"

| | re-asked | of them, on a TAUGHT word | static (customer loses MAX) |
|---|---|---|---|
| **before** | **5/14** | 3 ("youthful" ×3, all cyborg — the example's own word) | 0 |
| **after** | **1/14** | 0 (the one refusal was "younger", the model's own word) | 0 |

The before arm's other two: `"the set"` (a NEVER_WRITTEN word, guard doing
its job) and `"in his fifties"` on a brief stating *"late forties to
fifties"* — an observation about range-stating briefs, declared and NOT acted
on (a guard change is his (b) bar, and the `register.refusals` rows now make
that measurement free on real usage).

## The register half — verbatim keeps and the #327 classes (cyborg, n=8/arm)

| | verbatim keeps (12 exact seed phrases) | wardrobe cues | skin-stack cues | words |
|---|---|---|---|---|
| **before** | 7–12/12, mean **10.4** | one draft hit 3 | 4 drafts hit | 212–287 |
| **after** | 11–12/12, mean **11.8** | **0 across all 8** | 5 drafts hit (1 each, one 2) | 191–343 |

The customer's exact phrases now survive nearly whole, wardrobe invention on
a brief that names no clothing went to zero, and the paragraph did NOT
collapse toward LOW — length and heat held, which is the thinner-sheet risk
his bar exists to catch. Skin-stack cue hits are flat (loose single-word cues;
the before arm's were the measured #327 shape). Block-language cue hits rose
2→4 on loose cues, but read in context every after-arm hit is metaphor ("a
face that has taken years of hard light", "reads instantly on camera") where
the before arm's was a genuine restatement of the block's own negatives ("no
sheen, no symmetry, no youth in the skin" — before-s4's draft). Declared, not
suppressed.

## The frames — 4/arm from each arm's FIRST draft, his list, my eye

`output/477-max/477-before-after-strip.png` (also his eye gallery). Read
against his #327 list: **both arms** deliver the eight stated facts legibly,
one coherent weathered-porcelain skin read, faint glow. **After** is better
on *costume generic or absent* (before-s0 wears a full tactical strap
harness — the exact kit his #327 complaint named; three of four after frames
are plain dark jackets) and holds the temple ports on his RIGHT 4/4 vs 2/4
before. The glow's eye-side lands on the image's right on 7 of 8 frames
across BOTH arms — the known per-side paint behaviour of the engine, shared
equally, not moved by this change.

## Spend (house money, THE SPEND THRESHOLD)

Estimate posted on #477 before the first call: ≈$1.60. Actual: text
$0.12 + $0.09 (28 author calls, balance-attribution on the account-wide
meter, an indication not a spend meter) + frames $0.28 (8 renders, fal
14.86→14.58) ≈ **$0.49**. No render rows, no roll, no customer credits.

## Declared limits

One sitting, one evening's network; n=14 per arm; the MAN 2 brief is the
reader's description, not hand-typed customer prose; verbatim-keep cues are
exact substrings and under-count legitimate re-orderings; his eye on the
strip outranks every table here (law 9).
