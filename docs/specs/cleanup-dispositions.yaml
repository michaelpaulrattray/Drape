# THE CLEANUP MILESTONE'S DELETION DOOR.
#
# One row per symbol on the sweep's reading list. The verdict is the door;
# `argued` names the section of CLEANUP_MILESTONE_TRIAGE.md that argues it, and
# that section — not this line — is the reasoning. A HELD row names its blocker.
#
# Verdicts: KEEP · TAKE (read, and the reading says remove; not yet done) · TAKEN
# · HELD · FILED. A TAKE row deletes nothing by itself — the execution is its own
# commit, with the test question of 8c asked first, and a reviewer sees it there. An EMPTY verdict is the honest record
# of a symbol nobody has read yet, and `check-cleanup-dispositions.mts --strict`
# is green only when there are none left.
#
# TAKEN reads zero and that is correct rather than unused: a symbol removed by a
# previous shift is no longer on the sweep's list, so it has no row. The verdict
# exists for the row that is flipped in the same commit that deletes its symbol —
# git and the triage sections hold the history, and this table holds the door.
#
# A HELD OR TAKE ROW IS A CLAIM ABOUT THE REQUEST PATH, and since 2026-08-22 the
# checker holds it: a symbol under either verdict that has a production importer
# refuses as `rewired`. `stale` catches a row whose symbol went away; that one
# catches a row whose symbol CAME BACK, which no other refusal could see — a
# re-wired symbol simply drops off the sweep's reading list. It was born with a
# live hit in this table (`hairTakeSentence`, triage §25c), so it is not a
# hypothetical. **Do not flip a row to HELD to quiet it; check the callers.**

- symbol: verifyBackView
  file: server/casting/backViewGate.ts
  verdict: TAKEN
  why: its own docblock says "Back-compat alias (pre-D-46 callers/tests)"; the real gate verifyViewIdentity is wired at mintPackage.ts:41
  argued: 14b

- symbol: CAST_PUBLIC_ID_PATTERN
  file: server/casting/castPublicId.ts
  verdict: KEEP
  why: the public-id shape, pinned by its test so the alphabet and segmenting cannot drift silently
  argued: 5a/14c

- symbol: INK_ADD_RECIPE
  file: server/casting/evidence/composer/inkAddRecipe.ts
  verdict: HELD
  why: the placement-picker composer road, already marked retire in the Atlas
  argued: 8d/14a
  blocker: the composer road's module-sized disposition

- symbol: authorizeInkAddDescription
  file: server/casting/evidence/composer/inkAuthorization.ts
  verdict: HELD
  why: the placement-picker road's whole authorization module, with its own suite
  argued: 8d
  blocker: a module-sized retirement, not a symbol-sized one

- symbol: createInkCalibrationRecorder
  file: server/casting/evidence/composer/inkCalibration.ts
  verdict: HELD
  why: "an instrument for a calibration command that does not exist. READ 2026-08-22 (28e): the module is an ISLAND — its only importers at BIRTH (a63dbafe, 2026-07-27) were its own suite and a contract test that reads it as TEXT, and that is still true, so this is written-and-never-wired confirmed at the birth commit rather than assumed. It is also PINNED: r7-ink-add-d3-contract.test.ts asserts this name with toContain, driven by emptying the module — exactly one arm reddens"
  argued: 18a/28e
  blocker: "NOT the composer road's boundary — inkCalibration.ts is in no import closure at all, so the fixpoint reading 28c calls for cannot touch it. The real blocker is a DECISION: is R7-7D's founder visual/calibration gate still expected? The module is 4 exports and this is one of the 2 the sweep can SEE"

- symbol: evaluateInkCalibrationGate
  file: server/casting/evidence/composer/inkCalibration.ts
  verdict: HELD
  why: "an instrument for a calibration command that does not exist. READ 2026-08-22 (28e) with createInkCalibrationRecorder, same island, same birth commit. UNLIKE that one it is pinned by NOTHING outside its own suite — the contract test names the recorder and not this"
  argued: 18a/28e
  blocker: "NOT the composer road's boundary — see the row above. A DECISION about R7-7D's calibration gate, and the unit is the MODULE: its other two exports (createInkCalibrationRecord, summarizeInkCalibration) are consulted inside inkCalibration.ts itself, so the uncalled-export sweep structurally cannot list them — the isSensitiveAction shape the un-wiring differ was built for"

- symbol: probeOutcomeAllowsCanon
  file: server/casting/evidence/composer/inkRetryDecision.ts
  verdict: KEEP
  why: the retry decision's own predicate, in a module the composer road drives
  argued: 18

- symbol: assertEvidenceIntentStatus
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: assertEvidenceCandidateStatus
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: assertEvidenceCandidateAttemptStatus
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: assertEvidenceProbeOutcome
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: canTransitionEvidenceIntent
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: canTransitionEvidenceCandidate
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: canTransitionEvidenceCandidateAttempt
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: evidenceCandidateAttemptCost
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: deriveEvidenceCandidateBillingTruth
  file: server/casting/evidence/evidenceCandidateContract.ts
  verdict: KEEP
  why: the state machine written whole in one place while the SQL writers enforce it at every write
  argued: 6b

- symbol: planEvidenceMint
  file: server/casting/evidence/evidenceMint.ts
  verdict: KEEP
  why: the async wrapper its own module calls internally; its tests drive live planning through it
  argued: 18

- symbol: planEvidencePackageSync
  file: server/casting/evidence/evidencePackageAuthority.ts
  verdict: KEEP
  why: the async wrapper its own module calls internally; its tests drive live planning through it
  argued: 18

- symbol: commitBeginInkAddIntent
  file: server/db/inkAddIntents.ts
  verdict: HELD
  why: "builds intents for a whole downstream lifecycle suite; §10 item 7's third unpinned un-wiring, and it reached no row because the sweep's reading list excludes it — two TRACKED disposable scripts mention it, so it classifies as having a production mention. Added by hand 2026-08-22 so the rewired arm can pin it"
  argued: 8d/25d
  blocker: a disposable database — r7-ink-add-lifecycle-db.test.ts skips without TEST_DATABASE_URL, so re-pointing it onto commitBeginInkAnywhereIntent would ship an unverified rewrite

- symbol: buildInkAuthorizationProviderConfig
  file: server/casting/evidence/inkAddIntent.ts
  verdict: HELD
  why: arrived on the next sweep exactly as 8d predicted, when beginInkAddIntent was removed
  argued: 8d/14a
  blocker: the composer road's module-sized disposition

- symbol: ALL_SUPPORTED_INK_ANATOMY_TUPLES
  file: server/casting/evidence/inkAnatomyRegistry.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: isLimbInkZone
  file: server/casting/evidence/inkAnatomyRegistry.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: INK_RELEASE_POLICY_VERSION
  file: server/casting/evidence/inkReleasePolicy.ts
  verdict: KEEP
  why: what the policy's test pins so a policy change is a visible diff; the module is live in three places
  argued: 18

- symbol: stageCanonicalReferencePlate
  file: server/casting/evidence/referencePlateIngestion.ts
  verdict: KEEP
  why: its docblock FORBIDS callers until the reviewed C4 capability
  argued: 18

- symbol: getSessionCount
  file: server/casting/geminiGeneration.ts
  verdict: TAKEN
  why: its docblock says for monitoring and no monitor reads it; a door re-exported by geminiService that nobody imports from. TAKEN — with two tests, one of which cleared a key that did not exist on an empty map and could never fail
  argued: 20/17-family

- symbol: stopSessionEviction
  file: server/casting/geminiGeneration.ts
  verdict: TAKEN
  why: a graceful-shutdown hook nothing calls, and redundant by construction — the timer two lines above it is already unref'd so it never holds the process open. TAKEN — with its one test, which asserted only that it did not throw
  argued: 20/17-family

- symbol: roleForAuthorizedResult
  file: server/casting/identity/anchorSelector.ts
  verdict: HELD
  why: an unused convenience over the §7.2 role mapping; the live writers pass a literal
  argued: 18/18a
  blocker: M14's owner, with the other two anchor symbols

- symbol: commitAnchorReRoll
  file: server/casting/identity/identityCommit.ts
  verdict: HELD
  why: one of three uncalled symbols about who may stamp an anchor
  argued: 18a
  blocker: M14's owner, in one sitting with commitIdentityEdit and roleForAuthorizedResult

- symbol: commitIdentityEdit
  file: server/casting/identity/identityCommit.ts
  verdict: HELD
  why: one of three uncalled symbols about who may stamp an anchor
  argued: 18a
  blocker: M14's owner, in one sitting with commitAnchorReRoll and roleForAuthorizedResult

- symbol: compareModelSnapshotShadow
  file: server/casting/snapshotShadow.ts
  verdict: HELD
  why: its tests drive live code through it; re-pointing them is an unverified rewrite here
  argued: 17
  blocker: a disposable database — there is no docker on this machine

- symbol: assertStorageCleanupItemStatus
  file: server/casting/storageCleanupContract.ts
  verdict: KEEP
  why: a closed-value assertion beside assertStorageCleanupBatchStatus; the pair is the contract
  argued: 18

- symbol: resetStorageCleanupHealthReport
  file: server/casting/storageCleanupWorker.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: CROSS_AXIS_IMPLICATIONS
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: the axis sweep the suite drives over really-composed prompts, with sabotage arms that redden the right axis
  argued: 17

- symbol: suppressorsFor
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: the axis sweep the suite drives over really-composed prompts, with sabotage arms that redden the right axis
  argued: 17

- symbol: axesOnShelf
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: sweepComposedPrompt
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: the axis sweep the suite drives over really-composed prompts, with sabotage arms that redden the right axis
  argued: 17

- symbol: isTasteWritable
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: its own docblock: derived, never declared twice — the shelf's writability read off AXIS_REGISTRY
  argued: 6a

- symbol: AXIS_REGISTRY_BINDINGS
  file: server/castingV2/axisRegistry.ts
  verdict: KEEP
  why: the axis sweep the suite drives over really-composed prompts, with sabotage arms that redden the right axis
  argued: 17

- symbol: catalogueBornWorn
  file: server/castingV2/bornWornCatalogue.ts
  verdict: TAKEN
  why: "wire-or-retire, and the ruling came — his word of 2026-09-24: *Retire both. The paste road is gone; nothing reads these*. It catalogued what a cast was BORN with as segments, and the store it wrote to held ZERO rows on production and zero on dev, all time. ⚠ Its blocker read *a replacement control must land in the same commit* and it did: the sweep's second positive control is now stageCanonicalReferencePlate, chosen because its own docblock FORBIDS callers until the reviewed C4 capability — so unlike this symbol it cannot come clean by being wired OR retired. That is the second specimen this control has lost to a retirement (EYE_SHAPE_ENGINE was the first, D-248), and both were wire-or-retire symbols that were always going to move"
  argued: "#1160"

- symbol: deterministicBriefCompiler
  file: server/castingV2/briefCompiler.ts
  verdict: KEEP
  why: its docblock: an explicit seam for tests and for any caller that must compile without a network round trip
  argued: 5a

- symbol: censusIsOpen
  file: server/castingV2/callCensus.ts
  verdict: TAKEN
  why: an unused convenience beside the live censusSoFar; the caller it was written for never arrived. TAKEN — its line left the negative control, which keeps censusSoFar() === null saying the same thing
  argued: 17-family

- symbol: FRAMING_PREMISE
  file: server/castingV2/castingFrame.ts
  verdict: KEEP
  why: the framing sentence the out-of-frame table is keyed to — asserted, never assumed
  argued: 5a

- symbol: unmeasuredAmplitudes
  file: server/castingV2/changeAmplitude.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: COHORT_CONSTANT_MARKERS
  file: server/castingV2/cohortPhotorealHuman.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: courtSeparationFor
  file: server/castingV2/deliveryCourt.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: pairSlots
  file: server/castingV2/facePanel.ts
  verdict: KEEP
  why: the key a pair's row edits, derived from the slot vocabulary rather than spelled a second time
  argued: 6a

- symbol: faceScanCacheStats
  file: server/castingV2/faceScanService.ts
  verdict: KEEP
  why: its docblock: for tests and for the reliability report
  argued: 6c

- symbol: resetFaceScanCache
  file: server/castingV2/faceScanService.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: FACET_KEYS
  file: server/castingV2/facetCards.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: falGateStats
  file: server/castingV2/falConcurrency.ts
  verdict: KEEP
  why: its docblock: for the report and the tests — never a decision
  argued: 6c

- symbol: measuredPhrasings
  file: server/castingV2/falRegionReader.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: arrangementsWithPrecedent
  file: server/castingV2/hairArrangement.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: HAIR_STYLE_NAMES
  file: server/castingV2/hairStyles.ts
  verdict: KEEP
  why: every cut name, derived from the style tables; a hardcoded copy is what 6a exists to prevent
  argued: 6a

- symbol: neighbourPairs
  file: server/castingV2/heritageNeighbourhoods.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: splitByInheritance
  file: server/castingV2/inheritedVerdict.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: interpreterParseStats
  file: server/castingV2/interpreter.ts
  verdict: KEEP
  why: exported so a test can assert the parse-failure alarm FIRES rather than trusting it does
  argued: 6c

- symbol: interpreterRoleStats
  file: server/castingV2/interpreter.ts
  verdict: KEEP
  why: >-
    the same shape as interpreterParseStats beside it, for the same reason — a
    test asserts the counter MOVES rather than trusting it does. It counts the
    null-role re-ask, and counting is half the repair: `role` is the only field
    producing the CASTING CATEGORY block, and its absence was reported by the
    founder twice five months apart because nothing anywhere counted it.
  argued: 6c

- symbol: SYSTEM_PROMPT_FOR_TESTS
  file: server/castingV2/interpreter.ts
  verdict: KEEP
  why: the sweep's own by-construction positive control; its NAME is why it stays test-only
  argued: 0

- symbol: resetInterpreterForTests
  file: server/castingV2/interpreter.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: resetConceptReaderForTests
  file: server/castingV2/conceptDescribe.ts
  verdict: KEEP
  why: the same seam one module over (#231) - the reader now memoizes its own engine, and its
    allowance arm cannot run twice without dropping it; NAME says test-only
  argued: 6c/13a

- symbol: resetAuthorEngineForTests
  file: server/castingV2/promptAuthor.ts
  verdict: KEEP
  why: the same seam for the AUTHOR's own engine (#466) - it memoizes exactly as the reader's
    does, and the model-pin arms cannot run twice without dropping it; NAME says test-only
  argued: 6c/13a

- symbol: neighbourTableNames
  file: server/castingV2/maskedRefine.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: edgeTableNames
  file: server/castingV2/maskedRefine.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: segmentableRegionNames
  file: server/castingV2/maskedRefine.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: fringeTableNames
  file: server/castingV2/maskedRefine.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: rasterise
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: hairMaskFrom
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: eyeMaskFrom
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: requestMatte
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: eyeRegion
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: eyewearRegion
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: mergeRegions
  file: server/castingV2/maskGeometry.ts
  verdict: KEEP
  why: the declared fixture facility for tests that must not cost a credit; its vocabulary tests live algebra
  argued: 15d

- symbol: openKindQualifier
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindAmplitude
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindZoneScope
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindMovesItsEdge
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindUnfiledReason
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindIsPlural
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindFilesAsIdentity
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindDeparture
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: openKindSegmenterQuestion
  file: server/castingV2/openKindPolicy.ts
  verdict: KEEP
  why: the open lane's written answer sheet, quoted by the code that implements it independently
  argued: 7b/10

- symbol: shapePixels
  file: server/castingV2/referenceCentreline.ts
  verdict: KEEP
  why: the measurement thinToCentreline's own controls are built on, including the arm that would otherwise pass for the identity function
  argued: 8c

- symbol: DISPLAY_REGION_VOCABULARY
  file: server/castingV2/referenceSlotCatalogue.ts
  verdict: KEEP
  why: a policy record, deliberately NOT a route into the mint; slotSpecFor never reads display
  argued: 7b/10

- symbol: isFeatureSlot
  file: server/castingV2/referenceSlots.ts
  verdict: KEEP
  why: a predicate over parseSlot; the slot vocabulary asserted through its own parser
  argued: 6a

- symbol: presentPair
  file: server/castingV2/referenceSlots.ts
  verdict: KEEP
  why: its docblock: derived from the instances every time, so re-merging falls out for free — there was never a flag to clear
  argued: 6a

- symbol: axesOfFacet
  file: server/castingV2/refineFacets.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: unprotectedFacets
  file: server/castingV2/refinePreservation.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: REFUSAL_REASONS
  file: server/castingV2/refineRefusals.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: refusalCharge
  file: server/castingV2/refineRefusals.ts
  verdict: TAKEN
  why: an accessor over REFINE_REFUSALS, which the live path (refineInterpreter) indexes directly. TAKEN — its test was a MONEY promise over every reason and was re-pointed onto REFINE_REFUSALS[…].charge, not deleted
  argued: 17-family

- symbol: refusalReportClass
  file: server/castingV2/refineRefusals.ts
  verdict: TAKEN
  why: an accessor over REFINE_REFUSALS, which the live path indexes directly. TAKEN — its four mappings were re-pointed onto REFINE_REFUSALS[…].report
  argued: 17-family

- symbol: namesHerHairItself
  file: server/castingV2/refineService.ts
  verdict: TAKEN
  why: no caller and no test, beside the live asksToRemoveHerHair built from the same word sets; the road asks the sibling's question, never this one
  argued: 17-family

- symbol: refusalTallies
  file: server/castingV2/refusalCounter.ts
  verdict: FILED
  why: READ AND COUNTED (23g) — the premise was wrong: its one candidate reader does NOT import it (the world guard makes that impossible, so the bench restates the grouping in SQL), so it has ZERO consumers. Its WRITER is live in the paid refine path, and production holds 2 refusal rows with 0 rescued/upheld — the ratio it exists to measure is not yet readable
  argued: 13d/15b/23g
  owner: the reliability-report road — with a mechanical trigger rather than an intention: a tracked reader is built when production holds any rescued or upheld row

- symbol: heartbeatLine
  file: server/castingV2/reliabilityReport.ts
  verdict: TAKEN
  why: a one-line formatter beside the live formatReport; the tracked report script never calls it — and it was CORRECTED once for a defect it could never have shown anyone. TAKEN — with its two formatter arms; the rate, false passes and blockers are asserted on the report object elsewhere
  argued: 17-family

- symbol: namesRemoval
  file: server/castingV2/removalWords.ts
  verdict: TAKEN
  why: its own docblock says the service's backstop uses removalEvidence, which is live; this is the superseded coarse question. TAKEN — ten phrasings lived only in its test and were carried onto removalEvidence under the verdict it ACTUALLY returns; two of the six positives are ambiguous, which the one-bit question could not say
  argued: 17-family

- symbol: AMBIGUOUS_WORDS_FOR_CORPUS
  file: server/castingV2/removalWords.ts
  verdict: KEEP
  why: the test seam over AMBIGUOUS_REMOVAL_WORDS, which is private; it was BORN of namesRemoval's removal, when that symbol's ten phrasings were carried onto removalEvidence and the surviving corpus test needed the module's own list. Deleting it pushes that test onto a hand-copied list — law 4, a second list shadowing the source, which is the exact defect the carry was protecting against
  argued: 6c

- symbol: facetsWithUnreliabilityPrior
  file: server/castingV2/renderVerification.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: unreadFacts
  file: server/castingV2/renderVerification.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: resetCastingEngineForTests
  file: server/castingV2/rollEngine.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: pruneSegmentFacet
  file: server/castingV2/segmentPrune.ts
  verdict: TAKEN
  why: "a declared half-landing whose user promise removeStep already serves. Its blocker asked whether the segment-drop road still had a job; his 2026-09-24 ruling answered that for the whole road rather than for this symbol. Never had a production caller"
  argued: "#1160"

- symbol: namingTableFacets
  file: server/castingV2/segmentsOnFace.ts
  verdict: TAKEN
  why: "a derived view its test asserted the naming TABLE through. KEEP was the right answer while the table existed; slice 2 deleted the module, the table and the test in one act, so there is no hardcoded list left for it to be pushed onto"
  argued: "#1160"

- symbol: ACCESSORY_FACET
  file: server/castingV2/segmentsOnFace.ts
  verdict: TAKEN
  why: "the one facet id the module was asked about by name. ⚠ Read at the bytes before it was cut and it had NO consumer at all — not even a test — so by the end it was not keeping anything honest. Dies with its module"
  argued: "#1160"

- symbol: resetSignEnginesForTests
  file: server/castingV2/signEngine.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: BIAS_DEFERRAL_CLAUSE
  file: server/castingV2/stylingResolution.ts
  verdict: KEEP
  why: the clause every bias line ends on, exported so tests pin the contract
  argued: 5a

- symbol: exemptSubjects
  file: server/castingV2/subjectQualifiers.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: VACANCY_KINDS
  file: server/castingV2/vacancyPhrases.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: facetsNeedingMagnification
  file: server/castingV2/verificationDetail.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: INSTRUCTION_MAY_OVERRIDE
  file: server/castingV2/zoneScope.ts
  verdict: FILED
  why: RE-READ AT THE ARTIFACTS (23h) and 7a HOLDS — scopedZone takes no instruction argument, so nothing consults this table. Two additions: it is LIVE for every user except the founder (maskedRefine is the paste road and repaint scope is users:1), and it is a hand-written mirror beside a DERIVED table (ZONE_SCOPE comes from the facet cards), so it can drift from the defaults it quotes with only a mirror test watching
  argued: 7a/10a/23h
  owner: the zone-scope road's owner — the repair may be that the road retires, which is a design answer and not a cleanup pick

- symbol: OPEN_QUESTIONS
  file: server/castingV2/zoneScope.ts
  verdict: KEEP
  why: an empty map kept deliberately, founder-ruled 2026-08-06
  argued: 7c

- symbol: isDistributed
  file: server/castingV2/zoneScope.ts
  verdict: KEEP
  why: derived over ZONE_SCOPE — the facet's own scope, read rather than restated
  argued: 6a

- symbol: isBilateral
  file: server/castingV2/zoneScope.ts
  verdict: KEEP
  why: derived over ZONE_SCOPE, and the declared-intent instrument's negative control
  argued: 6a/5a

- symbol: scopedFacets
  file: server/castingV2/zoneScope.ts
  verdict: KEEP
  why: a derived view the test asserts the TABLE through; deleting it pushes its test onto a hardcoded list
  argued: 6a

- symbol: removeEdgesForItems
  file: server/db/boardEdges.ts
  verdict: TAKEN
  why: SUPERSEDED, not merely uncalled — the three deletion paths were fixed forward and none of them could use it. It deletes by bare item id with no ownership predicate, so wiring it would have satisfied 13c's sentence while breaking enforcement invariant 1 inside the commit fixing a deletion path
  argued: 13c/23d

- symbol: listSegmentHistory
  file: server/db/castingV2Segments.ts
  verdict: TAKEN
  why: "the reader a db suite asserted the retire-not-delete asymmetry through. ⚠ Its KEEP reason was the SUITE, and the suite dies with the store — so this row was a KEEP that quietly depended on the thing #1160 retires, and the checker found it rather than a shift noticing. Taken in slice 3 with the rest of the store's read paths; the asymmetry it proved (a retired segment's row survives as evidence of a delivered render) is a fact about a table nobody writes any more"
  argued: 6a/8c, taken under "#1160"

- symbol: listOrphanedVariants
  file: server/db/castingV2Variants.ts
  verdict: FILED
  why: READ AND COUNTED (23f) — it would return NOTHING today (0 orphaned variants in both worlds, detector passing) and half its predicate is dead by construction, since variant expiresAt is written by nothing. Still the retention road's call: the fix is either the PREDICATE or the COLUMN, and wiring it as-is installs a safety net with one arm tied behind it
  argued: 13c/13d/23f
  owner: the retention/purge owner — a sweep with no reader is collected-never-asserted in storage form, and only that road can say whether the purge should run it

- symbol: dbReferencePlateIngestionPersistence
  file: server/db/evidenceIngestion.ts
  verdict: KEEP
  why: the database half of the interface stageCanonicalReferencePlate takes, and that road's docblock FORBIDS callers until the reviewed C4 capability
  argued: 18

- symbol: inspectOwnedInkAddAvailability
  file: server/db/inkAddIntents.ts
  verdict: KEEP
  why: read-only owner-scoped capability truth proven by its own db suite while the mutation repeats every proof under the model lock
  argued: 6b

- symbol: getStorageCleanupBatchByOperation
  file: server/db/storageCleanup.ts
  verdict: KEEP
  why: inspection the deletion and worker db suites assert the live worker's writes through
  argued: 6b/13d

- symbol: getStorageCleanupItemsForBatch
  file: server/db/storageCleanup.ts
  verdict: KEEP
  why: inspection the deletion and worker db suites assert the live worker's writes through
  argued: 6b/13d

- symbol: _clearCooldowns
  file: server/monitoring/healthMonitor.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: _getCooldownCount
  file: server/monitoring/healthMonitor.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: recordGlobalFailedLogin
  file: server/security/rateLimit.ts
  verdict: KEEP
  why: WIRED 2026-08-19 by founder ruling ("wire and explain in plain english") — server/security/loginAttackAlert.ts is the call site the helper never had, reached from both failed-login exits
  argued: 9/13b

- symbol: markGlobalAttackAlertSent
  file: server/security/rateLimit.ts
  verdict: KEEP
  why: WIRED 2026-08-19 by founder ruling ("wire and explain in plain english") — server/security/loginAttackAlert.ts is the call site the helper never had, reached from both failed-login exits
  argued: 9/13b

- symbol: shouldSendGlobalAttackAlert
  file: server/security/rateLimit.ts
  verdict: KEEP
  why: WIRED 2026-08-19 by founder ruling ("wire and explain in plain english") — server/security/loginAttackAlert.ts is the call site the helper never had, reached from both failed-login exits
  argued: 9/13b

- symbol: _clearPendingActions
  file: server/slack/slackApproval.ts
  verdict: TAKEN
  why: was KEEP as a test-only seam; deleted with server/slack (#800, the Slack retirement) — nothing calls, nothing exports
  argued: 6c/13a

- symbol: _clearDedupCache
  file: server/slack/slackCore.ts
  verdict: TAKEN
  why: was KEEP as a test-only seam; deleted with server/slack (#800, the Slack retirement) — nothing calls, nothing exports
  argued: 6c/13a

- symbol: getDedupCacheSize
  file: server/slack/slackCore.ts
  verdict: TAKEN
  why: was KEEP as the read half beside the _clearDedupCache seam; deleted with server/slack (#800, the Slack retirement)
  argued: 6c

- symbol: allowColdImports
  file: server/testing/suiteClocks.ts
  verdict: KEEP
  why: server/testing/ is test infrastructure by directory
  argued: 13a

- symbol: allowTreeSweeps
  file: server/testing/suiteClocks.ts
  verdict: KEEP
  why: "server/testing/ is test infrastructure by directory (the allowColdImports precedent beside it). It is the clock for suites that sweep the source tree — #233's scheduling half, enrolled on measured casualties rather than on a stopwatch"
  argued: 13a

- symbol: cataloguedFeaturesWithRegion
  file: server/castingV2/viewFeatureWords.ts
  verdict: KEEP
  why: the totality control's only door — it is how a catalogue feature added without a body region arrives as a failing test instead of a silent regionUnknown
  argued: 13a

- symbol: baseUrlOf
  file: server/testing/fetchablePort.ts
  verdict: KEEP
  why: server/testing/ is test infrastructure by directory; it is the one place the loopback URL of an ephemeral listener is spelled, across five suites
  argued: 13a

- symbol: clearAllUserSessions
  file: server/wardrobe/vtoSession.ts
  verdict: FILED
  why: the live path is per-session clearSession from the wardrobe route; nothing clears a whole user's VTO sessions, and whether account deletion should is the wardrobe road's question
  argued: 13c/20
  owner: the wardrobe road — whether account deletion clears a whole user's VTO sessions is that road's question

- symbol: hairColourFacetsMissedByTheColourTake
  file: server/castingV2/hairReferenceTake.ts
  verdict: KEEP
  why: the heading fence's only door — it is how a future hair card headed COLOUR arrives as a failing test instead of silently joining the style take and leaking the one property that take promises to leave alone
  argued: 13a

- symbol: hairTakeSentence
  file: server/castingV2/hairReferenceTake.ts
  verdict: KEEP
  why: "ITS BLOCKER WAS DISCHARGED AND THE ROW WAS NEVER FLIPPED (found by the rewired refusal on the day it was built, 2026-08-22). HELD until then, waiting on the hair crop chunk; refineService.ts:143 imports it and :5280 calls it, which IS that chunk. Ruling 5's artifact — the sentence riding a hair crop, claiming the cut and explicitly not the colour — is now on the request path and pinned by hairReferenceTake.test.ts"
  argued: 25c

- symbol: hairTakeAdmits
  file: server/castingV2/hairReferenceTake.ts
  verdict: HELD
  why: the positive-admission gate that keeps a colour a reference read SAW out of a style-scoped ask; the read is what gives it something to refuse
  argued: 13a
  blocker: the hair read chunk, which is the first thing that can hand it a facet to judge (fable-1071 §5)

- symbol: hairColourReadOutcomeFor
  file: server/castingV2/hairColourFromReference.ts
  verdict: HELD
  why: the demand row's column spelling for a hair colour read, the mirror of makeup's referenceReadOutcomeFor; written with the reader so the enum walk exists before the migration rather than after
  argued: 14a
  blocker: the read's own procedure, which lands with migration 0044's outcome values (no_hair_visible, no_colour_readable)

- symbol: hairTakeIsAmbiguous
  file: server/castingV2/hairReferenceTake.ts
  verdict: HELD
  why: marks the open edge of the founder's 2026-08-19 no-question ruling — "copy the hairstyle but keep her colour" names two takes, counts as neither, and now falls to the whole lot, which takes the colour his own amendment protects
  argued: 14a
  blocker: the ruling on how a two-take ask is answered; the arm pinning today's behaviour cites this symbol by name

- symbol: findSeam
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: the composite discriminator, measured over his six specimens and now consulted on every attached picture; live on the paid refine path since the crop road was wired — a hair reference is cut, guarded and sent from `refineCandidate` itself
  argued: 14a

- symbol: panelsOf
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: turns a seam into the panels the segmenter is asked about separately, which is what stops a model choosing half a customer's reference on her behalf; live on the paid refine path since the crop road was wired — a hair reference is cut, guarded and sent from `refineCandidate` itself
  argued: 14a

- symbol: unionBox
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: the carrier's extent over hair and form together, refusing two masks in different spaces rather than resolving them; live on the paid refine path since the crop road was wired — a hair reference is cut, guarded and sent from `refineCandidate` itself
  argued: 14a

- symbol: composeCarrierPixels
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: the carrier itself — hair in its own pixels, the head flat-filled and unrecoverable; live on the paid refine path since the crop road was wired — a hair reference is cut, guarded and sent from `refineCandidate` itself
  argued: 14a

- symbol: carrierPicturesScale
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: REFUSES a carrier whose form is a rounding error, and it is consulted before the claim on the request path — invariant 7 satisfied rather than asserted
  argued: 14a

- symbol: encodeCarrier
  file: server/castingV2/hairReferenceCrop.ts
  verdict: KEEP
  why: encodes a composed carrier, separated so the composition stays pure; live on the paid refine path since the crop road was wired — a hair reference is cut, guarded and sent from `refineCandidate` itself
  argued: 14a

- symbol: cutHairCarrier
  file: server/castingV2/hairReferenceCutter.ts
  verdict: KEEP
  why: the crop road's orchestration — the seam, one hair question per panel, the largest panel carrying, the scale read and every free refusal; called by `refineCandidate` before the claim
  argued: 14a

- symbol: mintHairCarrier
  file: server/castingV2/hairReferenceCutter.ts
  verdict: KEEP
  why: writes the carrier manifest-first so bytes cut from a photograph of a person are never at a public address with nothing naming them; called beside the cut
  argued: 14a

- symbol: SECOND_VIEW_UNUSED_NOTE
  file: server/castingV2/hairReferenceCutter.ts
  verdict: KEEP
  why: what a customer is told when her two-view reference had one view carry; joined into the delivered take's owed-lines list
  blocker: the refine road's reference lane, which is where a note reaches her
  argued: 14a

- symbol: effectiveEnumMembers
  file: server/testing/migrationColumns.ts
  verdict: KEEP
  why: the sequence reader's refusing half — two re-aimed ink schema suites are its only importers, and it throws rather than returning [] on a non-enum so a caller cannot get a clean wrong green
  argued: 6c/13a

- symbol: resolveInkPlacement
  file: server/castingV2/inkPlacementResolve.ts
  verdict: KEEP
  why: the blocker cleared — `inkReferenceTake.ts` resolves the placement a model read out of her sentence, so the closed-first vocabulary is now what decides whether her word is measured or her own
  argued: V3B §7.11 (design opus-822, ruled fable-1115; the take ruled fable-1120)

- symbol: inkPlacementTallyValue
  file: server/castingV2/inkPlacementResolve.ts
  verdict: HELD
  why: the demand row's own value, sentinel and all; its writer is stage 4 and that writer is itself dead while MANNEQUIN_ROAD_DEFERRED is true
  argued: V3B §7.11 (design opus-822, ruled fable-1115)
  blocker: stage 4 — the tally writer, which cannot be reached at all until the mannequin road resumes

- symbol: PERSON_REGION
  file: server/castingV2/inkReferenceCrop.ts
  verdict: KEEP
  why: build 3a's licence word, and it is a measured value rather than a label — `person` cannot tell a drawn person from a photographed one (wrong in BOTH directions across two design sheets) and `human skin` separated 6/6; its own suite pins the string so a tidy back to `person` reddens
  argued: V3B §7.12 (measured opus-833/835, ruled fable-1126/1127/1129)

- symbol: cropClearsMinimumEdge
  file: server/castingV2/inkReferenceCrop.ts
  verdict: KEEP
  why: the completeness guard invariant 7 requires be consulted on the request path, deriving its floor from the upload door's own INK_DESIGN_MIN_EDGE rather than a second number; `cutInkDesign` consults it before the pixels are touched, and drives it from both sides of the floor one pixel apart
  argued: V3B §7.12 (design opus-830, ruled fable-1123 §3)

- symbol: cutOutPixels
  file: server/castingV2/inkReferenceCrop.ts
  verdict: KEEP
  why: the masked cutout itself — a bounding rectangle here is the fidelity violation fable-1052 ruled out twice, and this is the boring loop that replaces two clever idioms which BOTH silently returned an unmasked crop; called by `cutInkDesign`, whose containment arm counts every opaque pixel against a DIAMOND mask so a silent return to a rectangle reddens
  argued: V3B §7.12 (ruled fable-1052, built opus-838)

- symbol: cutInkDesign
  file: server/castingV2/inkReferenceCutter.ts
  verdict: KEEP
  why: build 3a.1's orchestration — the two measured questions asked with absentIsAnswer, the fail-closed routing, the completeness guard and five free refusals that share no sentinel; WIRED by 3a.2's upload wire, which calls it before the manifest so what lands at `casting_ink_designs.storageKey` is the design and not the photograph, behind `CASTING_INK_CUT_SCOPE`
  argued: V3B §7.12 (design opus-837, built opus-840; the wire ruled fable-1133 §3a)

- symbol: inkSideSlotKey
  file: server/castingV2/referenceSlots.ts
  verdict: KEEP
  why: the ink lane's per-side key, spelled ONCE beside its sideless sibling and its parser so no call site writes the prefix; today only `upperArm` is per-side and the only caller is the catalogue's own suite, because the road that mints a real ink slot is the refine wire. Deleting it would not save the spelling — it would move it into whichever call site needed a side first, which is the drift the open lane's `openSideSlotKey` exists to prevent
  argued: V3B §7.12 (shape (a) countersigned fable-1137 §2, built opus-848)
  owner: WIRED — `slotsForFacet`'s `perPlacement` branch composes the sided key for every per-side ink ask

- symbol: inkTakeSentence
  file: server/castingV2/inkReferenceTake.ts
  verdict: KEEP
  why: shape (e) — what a design reference is allowed to give her, the hair road's required `RecipeSource.scope` discipline transplanted. It claims the artwork and disclaims skin, skin tone, body shape, pose and lighting, and its arms drive the composed OUTGOING PROMPT rather than the string. Uncalled in production for one reason only: nothing assembles an ink source yet
  argued: V3B §7.12 (shape (e) countersigned fable-1137 §2, built opus-848)
  owner: WIRED — the ink branch composes it in front of her own face and the recipe carries it; the wire arm asserts both halves in the OUTGOING prompt

- symbol: inkDesignForAsk
  file: server/castingV2/inkDesignForAsk.ts
  verdict: KEEP
  why: the one owner of WHICH of her designs an ask means — a customer's sentence names a place and a Cast may hold eight, so the choice has to be made somewhere and this is the somewhere. It refuses NAMED rather than defaulting to the most recent (the unowned-axis class with a timestamp for an alibi), and it is the pre-claim half of the disposition door whose predicate it shares with `recipeAssembler` — sabotaging `inkDesignWasExamined` reddens arms in BOTH suites, which is what one owner looks like from the outside. Uncalled in production for one reason: the refine wire's entrance is the next chunk
  argued: V3B §7.12 (roads weighed opus-848 §7, ruled fable-1145 §4; two doors ruled fable-1146 §3)
  owner: WIRED — `refineService`'s ink branch calls it before the claim, and four arms at the wire drive its four answers (ride, none, ambiguous, unexamined) through the real service

- symbol: deltaCarriesDeliveredInk
  file: server/castingV2/inkApplied.ts
  verdict: KEEP
  why: the delivered-crop fence's own instrument, and it is a TEST-ONLY symbol by design rather than by neglect — its whole job is to answer "did a model's reply TRY to name a crop of this customer's body", which is the question `readDeliveredInk` deliberately cannot answer (a malformed attempt reads as "nothing usable here", and an instrument reporting that as absence would certify the fence over a hole). Its sibling `deltaCarriesAppliedInk` is identical in every respect and is absent from this table only because the sweep counts a self-reference and that one happens to be named in its own module's header. Deleting it would leave the strict reader's blindness provable in one direction only
  argued: V3B §7.12 (migration 0050, ruled fable-1197 §1; the both-directions condition is fable-1167 §2a inherited)
- symbol: deltaCarriesAskedInk
  file: server/castingV2/inkApplied.ts
  verdict: KEEP
  why: the third half's fence instrument, and TEST-ONLY by design exactly as `deltaCarriesDeliveredInk` above is — its whole job is to answer "did a model's reply TRY to name the words of a slot it was not asked about", which `readAskedInk` deliberately cannot answer (a malformed attempt reads as "nothing usable here", and an instrument reporting that as absence would certify the fence over a hole). Deleting it would leave the strict reader's blindness to `inkAsked` provable in one direction only, on the field that keys which sentence painted which tattoo
  argued: CASTING_V2_MULTI_TATTOO_DESIGN.md §6 arm 2 (shape A, ruled fable-1494; the both-directions condition is fable-1167 §2a inherited)
- symbol: overlapPixels
  file: server/castingV2/inkReferenceCrop.ts
  verdict: KEEP
  why: the face exclusion's own ARM instrument, and test-only BY DESIGN — its whole job is to make the fence's property provable at the bytes ("the carried crop holds zero pixels of a stranger's face"), which is a reading of the produced object rather than a claim about the code that produced it. Both its callers are arms and that is correct: the production path SUBTRACTS (`subtractMask`) and never needs to count what it removed. Deleting it would push the count into an inline loop in each arm — two implementations of one measurement, which is the drift law 4 names, and it is exactly what the cutter's arm had before it was pointed at this owner
  argued: V3B §7.12 (the region-crop road, condition fable-1183 §2b, countersigned fable-1201 §4)

- symbol: captureCastingTwoPathsEnabled
  file: server/castingV2/castingV2Scope.ts
  verdict: TAKEN
  why: "DELETED with its whole flag — #203 slice 2 step (e), 2026-09-25 — after his word of 2026-09-24 took the variable off the service (Crew reply #206, card switch-06-two-paths-retire, verbatim and entire: *Unset it*). The flag governed nothing by then: slice 1 (`67f422b9`) closed the entrance and step (e) deleted the born road it was the door to. ⚠ The constant, the parser, the two error classes and the BOOT FENCE went in the same act, and they had to: `server/scopeParentChain.test.ts` holds the declared scope constants and the `validate…Environment` fences equal as SETS, so a fence cannot outlive its constant or the reverse. `scripts/lib/productionFlagPositions.mts`'s row was removed in the same commit, because that table's own population arm refuses a row for a variable the code no longer declares"
  argued: CASTING_V2_TWO_PATHS_DESIGN §3.2/§10
  wired: "WIRED in the WRITE PATH slice, then un-wired by slice 1 of its own retirement and deleted in step (e). The road this row records in full: dark landing (no caller, HELD) -> wired at `rollService.createRoll` -> callerless again when slice 1 stopped writing a path -> deleted. Three states on one symbol, and only the middle one was ever visible to an importer count"

- symbol: bornWardrobeLine
  file: server/castingV2/wardrobeLine.ts
  verdict: TAKEN
  why: "DELETED with the born road — #203 slice 2 step (e), the wardrobe/basics retirement's last step, on his ruling in advance (*yeah we will retire the wardrobe/basics path obviously*). ⚠ It had been WIRED and then became UNREACHABLE, which is a third state this row has now held all three of: slice 1 (`67f422b9`) made the compiler's `path` a named constant `null`, so from that commit its only caller was a ternary arm `null` could never take — a dead branch, not a dead symbol, which is why no importer count and no uncalled-export sweep could see it. `sheetBasicsSex` went with it, having had no other caller. Its read-side sibling `currentWardrobeLine` STAYS and is live on the refine and Sign roads: the columns are kept as the evidence of which rolls predate the author road, so reading a stored sentence outlives composing a new one"
  argued: CASTING_V2_TWO_PATHS_DESIGN §3.3/§4/§5
  wired: "WIRED in the WRITE PATH slice, then orphaned by slice 1 of its own retirement and deleted in step (e). `basicsWardrobeLine` is deliberately NOT taken with it — `inkSurfaceCoverage.ts` derives its two BASICS_LINES coverage rows from it, so a historical basics cast refined today still has its chest read from the one declaration rather than from copied strings"

# ── UNREVIEWED (fable-1435 §3, 2026-08-23) ────────────────────────────────
# The un-wiring timeline classifies these as `died` — production imported each
# one and then stopped. This table is the deletion authority s ledger, so a dead
# symbol with no row here is an unmapped door; but writing a verdict nobody has
# argued would launder an un-made decision as made. UNREVIEWED says the true
# thing: on the ledger, not yet read. It licenses no deletion, and the count is
# compared for EQUALITY against UNREVIEWED_CEILING, so it can only shrink.

- symbol: getUserPoints
  file: server/db/credits.ts
  verdict: TAKEN
  why: "last real call in d81ddb80/1d3cd614 (withAtomicCredits moved the balance read to getUserCredits); only mention since was the db/index.ts barrel line. TAKEN 2026-09-16 with #108 slice 1 — alias and barrel line deleted"
  argued: 35a

- symbol: getPointTransactions
  file: server/db/credits.ts
  verdict: TAKEN
  why: "a `45eb2e5f` rename alias (points→credits) whose every caller moved in that same commit; only mention since was the db/index.ts barrel line. TAKEN 2026-09-16 with #108 slice 1 — alias and barrel line deleted"
  argued: 35a

- symbol: addPoints
  file: server/db/credits.ts
  verdict: TAKEN
  why: "a `45eb2e5f` rename alias (points→credits) whose every caller moved in that same commit; only mention since was the db/index.ts barrel line. TAKEN 2026-09-16 with #108 slice 1 — alias and barrel line deleted"
  argued: 35a

- symbol: addTopupCredits
  file: server/db/billing.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: isIpBlocked
  file: server/db/ipBlocking.ts
  verdict: KEEP
  why: "self-consulted — blockIp calls it for its duplicate check (ipBlocking.ts:64), so it is on the admin block road today; the REQUEST-PATH check it was written for was never wired (the 2722bc5b importer was rateLimit.ts's checkIpBlocked wrapper, whose only consumer was a test — CLAUDE.md's 'never wired' road, re-read 2026-09-17), and wiring or binning that control is a founder decision, not a Janitor act"
  argued: 36a

- symbol: hasBodyArt
  file: server/casting/geminiPrompts.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: updateSchemaForIteration
  file: server/casting/geminiSchemaUpdater.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: reconcileSchemaWithImage
  file: server/casting/geminiSchemaUpdater.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: markModelAssetsStale
  file: server/db/models.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: stampBoardItemWithVersion
  file: server/db/boards.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: mintModelAtomically
  file: server/db/models.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: buildInkProjectionProbeRequest
  file: server/casting/evidence/inkProjectionComposition.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: parseInkProjectionProbeResponse
  file: server/casting/evidence/inkProjectionComposition.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: assessInkProjectionProbe
  file: server/casting/evidence/inkProjectionComposition.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: composeEditPrompt
  file: server/castingV2/refineDelta.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: readStepDeltas
  file: server/castingV2/refineService.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: COVERAGE_BANDS
  file: server/castingV2/maskGeometry.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: cornersFromEyeMasks
  file: server/castingV2/canthalTilt.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: liveTakes
  file: server/castingV2/railTakes.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

- symbol: OPEN_SLOT_PREFIX
  file: server/castingV2/referenceSlots.ts
  verdict: UNREVIEWED
  why: classified `died` by scripts/unwiring-timeline.mts — production imported it and stopped; nobody has read it since
  argued: unreviewed

# ── §29: the sixteen `shared/` symbols the sweep had never looked for.
# `shared/` was a CONSUMER root and never a SCAN root until 2026-08-24. No row
# below is ruled for removal — fable-1508 §1 granted a repair and a triage, and
# a removal ruling is a different act. Every verdict names what it rests on.

- symbol: isCastingPath
  file: shared/castingPaths.ts
  verdict: KEEP
  why: "defensive-by-design, and structurally redundant: membership is already enforced by the same array as mysqlEnum(\"path\", CASTING_PATHS) at drizzle/schema.ts:2075 and z.enum(CASTING_PATHS) at server/routes/castingV2.ts:1101. The guard is what derive-never-mirror made unnecessary — the wire is closed without it"
  argued: 29c

- symbol: isInkCutRoute
  file: shared/inkCutRoute.ts
  verdict: KEEP
  why: "read FIRST and separately (fable-1508 §2) because it is the only row of the sixteen on a road LIVE in production (CASTING_INK_CUT_SCOPE=users:1). A fence the tests hold shut, not invariant 7 with a customer behind it: the containment rule governing that road is its sibling inkDesignWasExamined, live at inkDesignForAsk.ts:378 and recipeAssembler.ts:1338,1739, and column membership is mysqlEnum(\"cutRoute\", INK_CUT_ROUTES) at drizzle/schema.ts:3234"
  argued: 29c

- symbol: isInkProvenance
  file: shared/inkProvenance.ts
  verdict: KEEP
  why: "same class as isCastingPath: mysqlEnum(\"provenance\", INK_PROVENANCES) at drizzle/schema.ts:3196 and :3464, plus z.enum(INK_PROVENANCES) at server/routes/castingV2.ts:568 and :744"
  argued: 29c

- symbol: isInkTemplateKind
  file: shared/inkTemplateKinds.ts
  verdict: KEEP
  why: "same class: mysqlEnum(\"templateKind\", INK_TEMPLATE_KINDS) at drizzle/schema.ts:3304 enforces membership from the same array"
  argued: 29c

- symbol: isCanonicalViewAngle
  file: shared/boardTypes.ts
  verdict: KEEP
  why: "same class, enforced at the wire rather than the column: z.enum(CANONICAL_VIEW_ANGLES) at server/routes/boardOps.ts:683 and :694. Its own docblock states the job — narrow a stored viewType back to the comp-card six — and six other places assert that six is six"
  argued: 29c

- symbol: isModelArchivedStatus
  file: shared/modelLifecycle.ts
  verdict: KEEP
  why: "defensive-by-design and NAMED BY A GUARD: server/modelLifecycleGuard.test.ts:83 refuses any hand-rolled archived comparison and points at this symbol by name. Production consistently reaches for its sibling isModelAvailableStatus (11 call sites across server and client), which is NOT its negation — modelLifecycle.test.ts:81 records that deriving available as !isModelArchivedStatus would fail open. A sanctioned vocabulary member the current call sites happen not to need"
  argued: 29c

- symbol: STATED_ONLY_EYE_SHAPES
  file: shared/castingRealization.ts
  verdict: KEEP
  why: "NOT an inert control — checked, because an exception list consulted by nobody is the shape that usually is one. It pins a real property against the real dealer: realizedAxes.test.ts:347 drives realizeEight across 24 heritage/age/sex combinations and asserts no drawn candidate ever receives a stated-only value. The roll enforces the rule by never drawing eyeShape at all (EYE_SHAPES has refine-path consumers only), and this constant plus that test are what would notice if it started"
  argued: 29e

- symbol: everyInkTuple
  file: shared/inkReleasedPlacements.ts
  verdict: KEEP
  why: "a deliberate test-facing enumerator over the placement x side vocabulary, used by inkReleasedPlacements.test.ts and inkUploadDoor.test.ts:32 to drive the door's whole population rather than a sample. Its module's docblock (:99) explicitly fences the SIBLING isInkTupleReleased from use outside the module; this one is the enumerator that fence leaves available"
  argued: 29e

- symbol: BadRequestError
  file: shared/_core/errors.ts
  verdict: KEEP
  why: "one of four convenience constructors over HttpError, declared as a set by a docblock that states their job (\"Throw this from route handlers to send specific HTTP errors\"). ForbiddenError is live at server/_core/sdk.ts:2; this product routes through tRPC, so the other three are an unused remainder of a complete vocabulary rather than three independent dead symbols. Was also reachable through the barrel shared/types.ts:7 (export * from ./_core/errors) until #106 deleted that never-imported barrel on 2026-09-16"
  argued: 29e

- symbol: UnauthorizedError
  file: shared/_core/errors.ts
  verdict: KEEP
  why: "see BadRequestError — same four-member set, same docblock, same barrel at shared/types.ts:7 (deleted with #106, 2026-09-16)"
  argued: 29e

- symbol: NotFoundError
  file: shared/_core/errors.ts
  verdict: KEEP
  why: "see BadRequestError — same four-member set, same docblock, same barrel at shared/types.ts:7 (deleted with #106, 2026-09-16)"
  argued: 29e

- symbol: FALLBACK
  file: shared/modelRegistry.ts
  verdict: KEEP
  why: "a convenience aggregate whose members production imports individually and by name — IMAGE_FALLBACK, TEXT_HEAVY_FALLBACK and TEXT_LIGHT_FALLBACK are all live in server/casting/geminiGeneration.ts. It restates nothing (the object references the same consts), so it cannot drift from them; it is simply the door nobody walks through"
  argued: 29e

- symbol: framingsWithout
  file: shared/bodyAnchorRegions.ts
  verdict: KEEP
  why: "no-consumer-found-by-any-reading, and that is a floor rather than a proof. Its live sibling anchorPresentsIn is wired at server/castingV2/viewFeatureWords.ts:256, and this is the inverse view of the same table, written (per its docblock) so customer copy can NAME the framings an ask will not show in instead of hedging. The copy it exists for has not been written. Kept because removal has not been argued, not because it is called — this is the softest row in §29"
  argued: 29e

- symbol: openReferenceIntents
  file: shared/referenceIntents.ts
  verdict: FILED
  why: "the derivation-with-no-consumer carried forward as a lesson by opus-1156 §3, and the reason this whole gap was found. It was specified as the source of the open lane's honest refusal and has one caller — its own test. fable-1505 Q5 ruled the reference-selector build is its consumer, DERIVED and never typed"
  argued: 29a/29e
  owner: "item 8, the open-lane reference selector — build design returns for countersign (docs/specs/OPEN_LANE_REFERENCE_SELECTOR_DESIGN.md, tracked at 6241f92d)"

- symbol: referenceIntentEntry
  file: shared/referenceIntents.ts
  verdict: FILED
  why: "the per-intent accessor beside openReferenceIntents, same module and same unbuilt consumer: the selector reads an intent's form (crop / words / mannequinPlate) to decide what a tap may take"
  argued: 29a/29e
  owner: "item 8, the open-lane reference selector — as openReferenceIntents above"

- symbol: MAX_RENDERABLE_EVIDENCE_IMAGES
  file: shared/evidenceDelivery.ts
  verdict: FILED
  why: "a CAP that enforces nothing. Its only consumer anywhere is server/r7-evidence-image-retry.test.ts:31, which asserts a TIMEOUT is >= 4x it — so the number is asserted about and applied to no request path (invariant 7's own sentence). No card and no founder item, ruled fable-1508 §3: R7_EVIDENCE_COMPOSER_SCOPE is off in production, so no customer stands behind it. The condition is the row: a dead control keeps a live reputation exactly while its road is off"
  argued: 29e
  owner: "the sitting that turns R7_EVIDENCE_COMPOSER_SCOPE on — it inherits this question by name, and must decide whether the cap is enforced or deleted before that scope serves anybody"

- symbol: resetCrewBriefingCacheForTests
  file: server/crew/crewBriefing.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: resetErrorTrackerForTests
  file: server/monitoring/errorTracker.ts
  verdict: KEEP
  why: a seam whose own NAME says test-only; the sweep cannot see a convention and a human can
  argued: 6c/13a

- symbol: replyIsAcknowledged
  file: server/crew/crewBriefing.ts
  verdict: KEEP
  why: "the ONLY definition of 'seen by the crew' (CREW_TAB_DESIGN.md §1) — a pure function extracted so acknowledgement has somewhere to be tested (§9 arm 6). The client renders the same rule from the same deployed list; a drift between them is caught at the page, and the export is the server-side home of the meaning"
  argued: 6a

- symbol: MAX_SHEET_CHECKLIST
  file: server/castingV2/promptAuthor.ts
  verdict: TAKEN
  why: "deleted with the MAX author (#535, his 'build it' 2026-09-06): the imagination level is gone, no MAX sheet can be rolled, and a judging checklist for a road that no longer exists would be a document confident about code that moved underneath it"
  argued: "REIMAGINE_DESIGN_2026-09-06.md §3"

- symbol: ageContradictionIn
  file: server/castingV2/seedFidelity.ts
  verdict: KEEP
  why: "the locked-age half of the Re-imagine trio check (#535): reimagineRefusal reads it directly, and it is re-exported by promptAuthor.ts for the existing importers; deleting it would blind the one mechanical age guard the press has"
  argued: "REIMAGINE_DESIGN_2026-09-06.md §3"

- symbol: readListedSource
  file: server/testing/listedSource.ts
  verdict: KEEP
  why: "server/testing/ is test infrastructure by directory (the allowColdImports precedent). Seven tree-walking suites read their listed entries through it so a file that leaves between the listing and the read cannot ENOENT the deploy rite on a clean tree (#223); its own suite drives the race, and the negative control proves it still throws everything that is not absence"
  argued: 13a

- symbol: BRAND_NAME
  file: server/casting/geminiPrompts.ts
  verdict: HELD
  why: "⚠ THIS ROW IS ABOUT THE SERVER TWIN ALONE, AND AS OF 2026-09-13 IT CAN BE (#274, the door half). It answered BOTH declarations of the name for as long as the door was keyed on a bare symbol, and its own text said so. The reading now, measured at the tree through `importersAt(file, symbol)`: `server/casting/geminiPrompts.ts`'s legacy `BRAND_NAME = 'DRAPE'` has **0** production importers — `geminiService.ts` re-exports it and nothing imports it back. It read as **2** the hour before, and both of those were `client/src/features/staff/StaffBar.tsx` and `client/src/pages/AdminFoundation.tsx` importing the CLIENT constant `client/src/foundation/brand.ts` (\"Klieg\") through `@/foundation` — two live consumers of a different file keeping a dead server export off every list. It was KEEP for exactly that reason and the verdict is HELD again now, which is what it was from 2026-08-30 until section 03 re-wired the client twin. The client constant is NOT this row's business any more and needs none: the sweep reads `client/` for importers and not for declarations, so it is never listed"
  argued: 34
  blocker: "#29, the legacy-studio retirement — the program's FINISH LINE, begun only on the founder's word, with the Atlas's retirement views governing every deletion. One blocker now, not the two this row used to carry: the second was the CLIENT twin's, and a row keyed on (file, symbol) no longer speaks for it"

- symbol: runHook
  file: server/testing/hookDriver.ts
  verdict: KEEP
  why: "server/testing/ is test infrastructure by directory (the readListedSource precedent, 13a), so a production-importer reading of it is zero by construction and says nothing. Three hook suites drive their gates through it — prePushGate, preCommitGate, atlasMergeDriver — and it is the whole repair of #640: it THROWS when a process cannot be started rather than returning the old `-1`, so a driver that never ran the hook cannot answer the question the arms are asking. Deleting it returns all three suites to reporting a spawn failure as a hook verdict"
  argued: "#640"

- symbol: runHookAsync
  file: server/testing/hookDriver.ts
  verdict: KEEP
  why: "runHook's overlappable twin, and on the same test-infrastructure footing as its sibling above (server/testing/ is test infrastructure by directory, the readListedSource precedent 13a — a production-importer reading of it is zero by construction and says nothing). server/selfInvocationCheck.test.ts drives it: that suite spawns one tsx child per module, and sequentially it took 5.9s on an IDLE machine against this class's 30s cap. The deploy rite runs the script guards before it pushes, crossed the cap under its own load, and REFUSED a correct tree with a message reading like a real violation. Deleting this returns that arm to the sequential loop and puts the rite back one bad moment from refusing a clean commit — and the repair it replaces, raising the cap, would cost the arm its ability to report a genuine hang"
  argued: "#943"

- symbol: runWithLimit
  file: server/testing/hookDriver.ts
  verdict: KEEP
  why: "the cap that keeps runHookAsync's overlap from becoming its own starvation, same footing. Eight tsx starts at once on a machine already running the rest of the suite trades one starvation problem for another, which is #943's whole subject — so the import arm runs four in flight, not Promise.all. It is also what keeps a SpawnFailure from being lost: every job is awaited inside its worker, where Promise.all would surface the first rejection and leave the siblings unhandled, and vitest reports an unhandled rejection against whatever file happens to be running"
  argued: "#943"

- symbol: requireShell
  file: server/testing/hookDriver.ts
  verdict: KEEP
  why: "the other half of #640, on the same test-infrastructure footing: it resolves Git for Windows' own sh.exe from the git on PATH, because `sh` resolves under Git Bash and NOT under PowerShell — which is how a shift runs pnpm preflight. Without it server/prePushGate.test.ts is 6/6 green from one shell and 5/6 red from the other on the same commit, and preflight can never come back green on this machine. It refuses by name when no shell exists rather than handing back one that cannot run"
  argued: "#640"

- symbol: workerCap
  file: server/testing/workerCap.ts
  verdict: KEEP
  why: "server/testing/ is test infrastructure by directory (the allowColdImports precedent). It is the ceiling vitest.config.ts reads for maxWorkers — vitest's own cores − 1, capped at 8 — because 19 workers on the founder's box starved the child-process suites into 13–22 timeouts a run and doubled the wall time, measured as a back-to-back pair (301 s / 2 reds at eight against 680 s / 22 at nineteen). Read by the config, never by product code, and server/workerCap.test.ts pins the wire at the config's bytes"
  argued: "#743"

- symbol: mergeAttributeChanges
  file: server/lib/boardOps.ts
  verdict: TAKEN
  why: "ARGUED AND CUT. #886 asked the one question #274 could not: did ALL the rules move, or was one lost on the paid path? Read rule by rule against the handlers - rule-1 (a gender change clears hairStyle/hairFade/facialHair) is `person.gender.buildPreferencePatch`; rule-2 (a style change resets its sub-selectors) is the `person.hair.style` extraResets; the ethnicity dual-write is `person.ethnicity.buildPreferencePatch` writing BOTH `ethnicityBlend` and the derived legacy string, with structuredEdit.ts:205 deriving the blend from prose for the other direction. All three arrived in 8b514bed (R6 Batch C, 2026-07-16), so NOTHING WAS LOST and this was a dead export, not a live gap. ITS ROAD IS PATH THREE, read at the bytes and not from the import graph: born WIRED in a5b1ddf0 (2026-07-11) with a real call site on the paid path - `resolveEngineChoices(mergeAttributeChanges(current, input.changes))` inside applyModelEdit - and UN-WIRED by c7cdc7ad (2026-07-28, `R7-7D D7: ship free evidence-aware Fork`), a commit aimed at something else entirely, which left no failing test and no error. Live 17 days, dead 47. It was invisible because boardOps.ts is namespace-imported (#274) AND because r7-ink-add-d7-contract.test.ts asserted its SOURCE TEXT, so the one instrument that touched it was holding it alive. The shared rules it delegated to are NOT dead - CastingWorkspace.tsx:229 merges the fork-to-edit draft through them - so the four test arms moved to shared/mergeCastingPreferenceChanges directly and every assertion is kept"
  argued: "#886"

- symbol: composeFromAssets
  file: server/casting/composeIdentityPayload.ts
  verdict: HELD
  why: "the D-30 strategy-(b) pure core, reached only by its own suite now that #108 slice 2b removed the ownership-checked wrapper (`composeIdentityPayload`, dark-born at every one of 378 timeline boundaries). The module carries the Atlas lifecycle `delete` (scripts/generate-architecture.mts) and has NO production importer left, so it is a whole-module retirement, which is #29's act and the Atlas's retirement view is its authority"
  argued: 37a
  blocker: "#29 — legacy-studio retirement is the FINISH LINE, begun only on the founder's word; a Janitor slice removes dead exports, never a legacy module"

# ── #1158 slice 2: THE INK STUDIO'S RETIREMENT LEFT FIVE EXPORTS WITHOUT A
# CALLER, and each one is here rather than deleted in the same commit for the
# reason the header gives: a TAKE row deletes nothing by itself, the execution is
# its own commit, and a reviewer sees the reading before the knife. All five lost
# the SAME caller — `uploadInkDesign` or the plate mint it called at step 5 — so
# they are one finding with SIX instances, not six findings.
#
# ⚠ THE SENTENCE ABOVE SAID "FIVE" AND THE TABLE HELD SIX. Slice 3 added
# `INK_DESIGNS_PER_CANDIDATE_REFUSAL` below without re-counting the paragraph, and
# two shift handoffs then carried "the five TAKE rows" forward. Read at the file
# on 2026-09-24 (slice 4e), which is the only place the number was ever true.
#
# ⚠ NONE OF THEM IS HELD, AND THAT IS DELIBERATE. The tempting blocker is *"the
# reference road may want this when he un-holds it"* (Crew reply #213, moved to
# N3), and it is precisely the thing his own rule from the switch sitting forbids:
# *"Folding a new capability into a retirement is how a half-built feature ships
# under a cleanup's name."* Nothing is kept here because a future road might want
# it; a road that needs a placement door when it wakes writes one against its own
# vocabulary. They are read, the reading says remove, and #1158 slice 4 removes
# them at the Atlas's retirement view.
#
# ⚠ AND THE CLAUSE ABOVE IS ABOUT THE STUDIO'S OWN DOORS, NOT ABOUT EVERY SYMBOL
# THE RETIREMENT TOUCHES. Executing these six ORPHANED two functions in
# `shared/referenceIntents.ts` — `referenceIntentWrongDoor` and
# `referenceIntentNotOpen`, whose only production caller was `inkIntentRefusal`.
# They are HELD below rather than taken, and the distinction is the whole reason
# the two rows exist: those six were the retired road's OWN machinery, and
# keeping them would be keeping a dead road's parts. The other two are the copy
# layer of a LIVE shared vocabulary whose siblings are on `candidateRetention`'s
# request path today. Whether a live shared module keeps its sentences is a
# decision about that module, judged on its own terms — which is his rule 1 read
# from the other end, not an exception to it.

- symbol: inkIntentRefusal
  file: server/castingV2/inkUploadDoor.ts
  verdict: TAKEN
  why: "the studio upload's FIRST door - 'no extraction without intent', asked before the placement and long before the bytes. Its only production caller was `uploadInkDesign`, retired 2026-09-24 by his ruling on card `switch-10-ink-studio` (*\"It retires with N2\"*). Driven today only by `inkUploadDoor.test.ts`, whose arms go with it; the reference road's own intent handling is in `referenceIntents.ts` and does not pass through here"
  argued: "#1158"

- symbol: inkPlacementRefusal
  file: server/castingV2/inkUploadDoor.ts
  verdict: TAKEN
  why: "the studio upload's placement/side door, answered against the master framing. Only production caller was `uploadInkDesign`. ⚠ It is NOT the placement narrowing's pin any more - #1158 slice 1 re-aimed `inkPlacementCoupling.test.ts` onto `InkDesignToRecord[\"placement\"]`, the input type every road must pass through, precisely so a text pin on one writer's file could not miss a second writer widening. So removing this function does not weaken that coupling, which is the one question worth asking of it"
  argued: "#1158"

- symbol: candidateBelongsTo
  file: server/db/castingV2InkDesigns.ts
  verdict: TAKEN
  why: "the FREE owner read the upload did before it spent anything (#860) - two segmenter calls and an R2 write used to run on a candidateId nobody had resolved. Its only production caller was `uploadInkDesign`'s `prove` dependency. ⚠ It was never the ownership AUTHORITY and removing it takes no control with it: invariant 1's authority is the locked read inside `recordInkDesign`'s own transaction, which is unchanged and still scopes the owner in the statement that writes. Driven by `castingV2-ink-design-db.test.ts` against a real database"
  argued: "#1158"

- symbol: readInkDesignCastIdentity
  file: server/db/castingV2InkDesigns.ts
  verdict: TAKEN
  why: "the identity read the PLATE MINT made to draw a design onto a form. Zero readers anywhere in the tree after slice 2 - not a test, not a script - because the mint was its only one and the mint is deleted. The plainest row of the five"
  argued: "#1158"

- symbol: recordInkPlate
  file: server/db/castingV2InkPlates.ts
  verdict: TAKEN
  why: "the ONLY writer of `casting_ink_plates`, called by `mintInkPlate` alone, and the mint is deleted. ⚠ The TABLE is NOT in this row and must not be taken with it: `candidateRetention` purges it and `castingV2InkDesignRemoval` clears it, so the module stays. ⚠ **Its READ path was named here as surviving and it no longer does** — slice 4f deleted `signService.carriedInkPlates` and `listCandidateInkPlates` with it, so the module is PURGE-ONLY; that sentence is corrected rather than struck because the next reader of this row needs the tree it is looking at. Production held zero plate rows all time, so nothing is orphaned by a table that can no longer gain one. Driven by `castingV2-ink-plate-db.test.ts` against a real database"
  argued: "#1158"

- symbol: INK_DESIGNS_PER_CANDIDATE_REFUSAL
  file: server/castingV2/inkUploadDoor.ts
  verdict: TAKEN
  why: "the studio upload's CAP sentence — what a customer was told when a Cast already held its eight designs. Its only consumer was the retired `ink.upload` route, which went on slice 1 and left the IMPORT behind; slice 3 removed that import and this row is the consequence the instrument asked for. ⚠ The NUMBER it interpolates, `INK_DESIGNS_PER_CANDIDATE`, is NOT in this row and must not travel with it: three live consumers read it (`inkReferenceMint.ts:93`, `referenceAttachDoor.ts:31` which derives `REFERENCE_PICTURES_PER_CANDIDATE` from it, and the door's own suite). ⚠ And the HELD reference road does not lose a sentence when this goes - it carries its OWN copy of the same words at `inkReferenceMint.ts:245`, which is why removing this one resolves a duplicate rather than creating a gap"
  argued: "#1158"

- symbol: listInkPlatesForDesign
  file: server/db/castingV2InkPlates.ts
  verdict: TAKEN
  why: "the studio's own per-design plate read, and the SEVENTH symbol of a six-row slice. ⚠ It reached this table by a law-7 sibling sweep rather than by the instrument, because the uncalled-export sweep CANNOT SEE IT: the only file naming it is `castingV2-ink-plate-db.test.ts`, which reaches it as `plates.listInkPlatesForDesign` off a namespace held in a variable (`plates = await import(...)`) — a fourth dynamic shape the reader does not resolve, so it appeared in neither TEST-ONLY EXPORTS nor NAMED BY NOBODY. `recordInkPlate` sat in the same blind spot and only reached the table by hand. Zero production callers, read across the whole tree 2026-09-24. Taken in slice 4e rather than filed, because the arms that drove it were the deleted writer's and leaving it would have made it invisible to every instrument at once"
  argued: "#1158"

- symbol: referenceIntentWrongDoor
  file: shared/referenceIntents.ts
  verdict: HELD
  why: "the sentence a customer reads when her feature is OPEN but served by another door. ⚠ ORPHANED BY #1158 SLICE 4e rather than found dead: its only production caller was `inkIntentRefusal`, the studio upload's intent door, deleted in that slice. It is NOT the studio's machinery — it is the copy layer of a live shared vocabulary whose siblings (`referenceIntentIsOpen`, `referenceIntentIngestionForm`, `REFERENCE_INTENTS`) are on `candidateRetention`'s request path today, so deleting it is a decision about that module and not a consequence of this retirement. Its suite drives it DIRECTLY and says why: the per-form derivation it carries pins a defect that is invisible until hair opens, and the door could never reach that branch"
  argued: "#1158"
  blocker: "the reference road's own intent door. He HELD that road on 2026-09-24 (Crew reply #213, verbatim: *\"Hold. The attach and picture road isn't finished. It moves to N3, after the engine court, and gets built on whichever engine wins.\"*). When N3 builds that door this row flips to KEEP if it calls these, or to TAKE if it writes its own — and either answer is one reading, not a guess"

- symbol: referenceIntentNotOpen
  file: shared/referenceIntents.ts
  verdict: HELD
  why: "the sentence a customer reads when her feature is RULED but not built — 'her hair', and the money promised back. Orphaned by #1158 slice 4e with `referenceIntentWrongDoor` above, same caller, same module, same argument; the two are one finding with two instances"
  argued: "#1158"
  blocker: "the reference road's own intent door — see the row above. Same hold (Crew reply #213), same two outcomes, and they are decided together because a road that asks what a picture is for needs both sentences or neither"

- symbol: assembleWithCarriedSegments
  file: server/castingV2/carriedSegments.ts
  verdict: TAKEN
  why: "the composite that pasted a face's kept segments back over a fresh render. ⚠ ORPHANED BY #1160 SLICE 1, deliberately and in the card's own order: entrance first, then read paths. Its only production caller was `refineService`'s paste road and that call is gone, so this is the retirement's designed halfway state rather than a find. It is NOT held for a reason of its own — there is no argument for keeping it — and it must not be read as a KEEP: slice 2 deletes `carriedSegments`, `segmentPersistence`, `bornWornCatalogue`, `segmentPrune` and `segmentAssembly` together. ⚠ `segmentAssembly` is the FIFTH and is not on the card's list of four: its only reaches are `carriedSegments` and `segmentPrune`, both retiring, so it falls with them. Its own suite still drives it until then"
  argued: "#1160"

- symbol: keepSegmentsFromRender
  file: server/castingV2/segmentPersistence.ts
  verdict: TAKEN
  why: "the segment store's only WRITER, called on every refine and — read at the rows 2026-09-25 — successful on none: `casting_segments` holds ZERO rows on production and zero on dev, all time, with 4 and 7 users as the positive control. The reason is structural rather than luck: it refuses without `image.evidence`, and `repaintOnce` (the road every production render takes under `CASTING_REPAINT_SCOPE=all`) declares that field `undefined`. ⚠ Orphaned by #1160 slice 1 in the card's order; `accessoryCorridor.test.ts` still drives it directly until slice 2 deletes the module"
  argued: "#1160"

- symbol: settleCarriedChecks
  file: server/castingV2/renderVerification.ts
  verdict: TAKEN
  why: "marked a verification check non-binding when the fact was pasted rather than painted, so a reader that could not find it did not spend a refusal. ⚠ ORPHANED BY #1160 SLICE 1, and it is the one of the three that does NOT die with a segment module: `renderVerification.ts` is the live verification net and stays. Both its arguments came from the composite's arithmetic, so with the paste road gone it can only ever be handed two empty lists — and its own first line is `if (carried.facets.length === 0) return verdict`, which is why removing the call was provably behaviour-preserving rather than argued. Deleting the function itself is a decision about `renderVerification`, not a consequence of this retirement, so it waits for slice 3. `renderVerification.test.ts` drives it directly ⚠ TAKEN in slice 3, and the decision it waited for was answered the way the row predicted: with the paste road gone it can only ever be handed two empty lists, so it was DEAD rather than merely orphaned. Its module survives untouched and so do the three helpers it used (facetIn, isMiss, okOf), each of which has other consumers — checked before the cut, not after. The 125-line describe block that drove it went with it: every arm passes a non-empty carried list, which nothing on any road can now produce."
  argued: "#1160"

- symbol: deliveredAnchoredSegmentsEnabled
  file: server/castingV2/castingV2Scope.ts
  verdict: TAKEN
  why: "CASTING_SEGMENTS_DELIVERED_SCOPE's gate, and the flag half of the pair his ruling retired. Its only production reader was segmentPersistence, deleted in this slice, so it is test-only from here. ⚠ It is NOT deletable with the modules: the flag is still SET on the service at users:1, and a scope chain's child refuses to boot when it reaches past its parent — so the variable leaves the service before the code that declares it, and both are slice 3 ⚠ TAKEN in slice 3, and the order the row warned about was honoured and PROVEN rather than asserted: CASTING_SEGMENTS_DELIVERED_SCOPE left the service before CASTING_SEGMENTS_SCOPE and both left before this code, driven through the real validateEnv() with the parent-first case as a negative control that had to name the right variable."
  argued: "#1160"

- symbol: recordEditPatchSegments
  file: server/db/castingV2Segments.ts
  verdict: TAKEN
  why: "the store's INSERT. Orphaned by deleting segmentPersistence, its only production caller. Held to slice 3 with the rest of the db module: the table is not dropped yet and a writer removed before its table is a migration question, not a code one ⚠ TAKEN in slice 3, and the migration question the row raised is answered by NOT answering it: the table is NOT dropped, because a DROP is a destructive migration and therefore the founder's own act. The purge helpers stay for exactly that reason."
  argued: "#1160"

- symbol: listLiveSegments
  file: server/db/castingV2Segments.ts
  verdict: TAKEN
  why: "one of the store's two reads, orphaned with carriedSegments. Held to slice 3 with its module ⚠ TAKEN in slice 3 with its module's read paths."
  argued: "#1160"

- symbol: retireSegmentFacet
  file: server/db/castingV2Segments.ts
  verdict: TAKEN
  why: "the store's retire-a-facet write, orphaned with segmentPrune. Held to slice 3 with its module ⚠ TAKEN in slice 3 with its module's write paths."
  argued: "#1160"
