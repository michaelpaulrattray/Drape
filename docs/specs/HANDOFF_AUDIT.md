# State note — held for the Fable system audit (2026-07-13)

**Held at `d883c7f` (main + local-migration, deployed).** A round-4 walk of the D-55 draft loop surfaced a *substrate* inconsistency, not a UX one: per-view iteration capabilities differ arbitrarily — the side view's refine prompt says it cannot be edited, the ¾ view's iterate box won't accept typing at all — so before any more symptom-by-symptom fixes, a full system audit takes over to fix on a map. **This session is holding: B1/B2 and the round-4 walk findings are NOT being touched.**

**Shipped (VC-R6 final r1→r3, all deployed):** the D-55 stays-draft loop is status-driven end to end — draft badge, no-name stays-draft, "Add views — she stays a draft", Core purchase keeps draft status, tattoo edits land on the target view, no fork ceremony on draft edits; r3 closed session-reentry corruption (reset on every close, F1), the D-38 draft-view paint gap (F2), the stale-writer's classifier dice-roll (permanent marks are now a deterministic identity/stale trigger, F3), and unified stale indication across the board mosaic and the studio strip (F5). F4 (reference-bleed propagation) and F6 (headshot-refresh-on-draft) are ratified — F4 gated behind a calibration run as a D-30 amendment pending calibration (detection ships, propagation does not); F6 applied (headshot stays non-refreshable, status-aware copy routes the draft case to iterate-in-environment). See `DECISION_LOG.md` (r2/r3 entries) and `REFERENCE_BLEED_ASSESSMENT.md`.

**Parked, for the audit to map (not fixed):** B1 — stale-count; B2 — mint-naming; the four round-4 walk findings (the per-view iteration inconsistency above is the substrate trigger; the founder holds the full enumeration for the audit). Proof harness for whatever the audit rules: `scripts/verify-canvas.mts` (free A–Z + FR/FL/SD1-12/NT; paid MK ~350cr, Y ~1600cr). Advisor subagent available from next session (`.claude/agents/advisor.md`).
