# The refine loader — the mockup he chose (2026-09-26)

Card **#55**. His word, verbatim: *"i just adjusted thew ripple. its perfect now file it"*.

`Particles.dc.html` is board E of the design canvas (https://claude.ai/artifact/X2vuitBb1WjVG2fpy5fDe9, private to him) exactly as he left it — including the values he set on the sliders: 1,700 particles, mouse reach 40 px, a ripple every 10 s. The `/_blob/…` image it references is the refine's waiting overlay picture (a blurred, desaturated, darkened frame); any blurred cast picture stands in for it.

Read the spec on the card before building; the source here is the reference for the motion, not code to ship. The build owes what the card names: the refine road publishing its real stage transitions, a frame budget measured on a mid laptop with the particle count capped by device (never by a customer-facing control), and his eye on the rendered frames before it ships.
