Layout Analysis - Reference vs Current Implementation

REFERENCE IMAGE 1 (Target):
- Left sidebar: Thumbnails at ~w-12 (small), vertically stacked
- Main image: Centered, large, with light gray background
- Tool buttons: On RIGHT EDGE of main image container, inside the image area
- Reference node: TOP RIGHT, above the "Next Stage" button, with dashed border
- Chat input: At BOTTOM of main image, pill-shaped, "PAINT AREA TO ERASE"
- View label: "FRONT CLOSE" at bottom LEFT of image
- Director's Note: Full width at very bottom
- Next Stage button: RIGHT side, middle-aligned with circular arrow button

CURRENT IMPLEMENTATION (Reference Image 2):
- Left sidebar: Thumbnails at w-24 (larger than reference)
- Main image: Centered but different proportions
- Tool buttons: On right edge but positioned differently
- Reference node: Position is correct (top-20 right-8)
- Chat input: Bottom center, similar style
- View label: Bottom left, similar
- Director's Note: Full width bottom

KEY DIFFERENCES TO FIX:
1. Left sidebar thumbnails should be smaller (w-12 instead of w-24)
2. Tool buttons should be positioned inside the image container, not floating outside
3. Reference node positioning looks correct but may need fine-tuning
4. Main image container should have more defined boundaries
