# Agent artifacts
.manus/
__manus__/

# Dependencies
**/node_modules
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# Theme-parity screenshot output (scripts/drive-foundation-theme-parity.mts)
.theme-shots/

# Drape Atlas explorer — a pure derivation of drape-architecture.json, which IS
# committed. Keeping the 500KB HTML out of git avoids doubling the churn on
# every source change for a diff nobody can usefully read; `pnpm
# architecture:generate` recreates it in seconds.
docs/architecture/index.html

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock
*.bak

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Database
*.db
*.sqlite
*.sqlite3

# M3 calibration output (real spend artifacts, founder-reviewed then disposed)
.calibration/

# Local agent scaffolding — never committed (founder rule). The mailbox was
# staged by name once (c47b27ba) and untracked again in 1f2d3011; this is the
# guard that stops it being possible rather than remembered.
.agents/
.codex/
.avela-agent-bridge/
CLAUDE.local.md
.claude/settings.local.json

# Untracked scratch that has never been committed and must not be stageable by
# accident (Janitor patrol #1, issue #96): `output/` holds drive-script
# artifacts — including minted session JWTs (`output/.tok*`, deleted 2026-08-26)
# — and `.playwright-mcp/` holds browser console logs and page snapshots from
# the Playwright MCP drives. Both were 0 tracked files the day this landed.
output/
.playwright-mcp/
